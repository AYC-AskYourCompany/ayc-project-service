server:
  port: 8077

keycloak:
  auth-server-url: http://localhost:8007/auth
  realm: ayc
  resource: ayc-service

security:
  cors:
    origins: http://localhost:4200

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: ayc
